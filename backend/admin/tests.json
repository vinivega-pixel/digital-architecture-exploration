{
  "tests": [
    { "name": "CORS preflight", "method": "OPTIONS", "path": "/", "expectedStatus": 200 },
    { "name": "Без токена доступ закрыт", "method": "GET", "path": "/", "expectedStatus": 403 }
  ]
}
